/**
 * A wrapper for labels and their associated `checkbox` or `radio` type input
 * to display them side by side.
 *
 * The class can be applied to implicit labels (i.e. the `input` is placed
 * inside the `label`) or to a wrapper element that contains both
 * the `label` and the `input`.
 */
export const choiceLabel = [
    ['choice-label', {
        'align-items': 'flex-start',
        'display': 'flex',
        'gap': '0.5em',
        'width': 'fit-content',
    }],
];
